<% provide(:title, 'Punti vendita') %>
<%= will_paginate %>

<table class="table table-striped">
      <thead>
        <tr>
          <th><span class="glyphicon glyphicon-barcode" aria-hidden="true"></span></th>
          <th><span class="glyphicon glyphicon-road" aria-hidden="true"></span></th>
        </tr>
      </thead>
      <tbody>

      <% @stores.each do |store| %>
        <tr>
          <th scope="row"><%= store.id %></th>
          <td><%= link_to store.place, store %></td>
        </tr>
      <% end %>
      
      </tbody>
</table>

<%= will_paginate %>